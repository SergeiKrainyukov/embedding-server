# 🚀 Embedding Server

Kotlin-сервер для создания текстовых эмбеддингов с использованием Ollama (nomic-embed-text).

## 📋 Требования

- **JDK 17+**
- **Ollama** с моделью `nomic-embed-text`

## 🛠 Установка Ollama

```bash
# 1. Установить Ollama (если ещё не установлен)
# macOS/Linux:
curl -fsSL https://ollama.com/install.sh | sh

# 2. Скачать модель для эмбеддингов
ollama pull nomic-embed-text

# 3. Запустить Ollama (обычно запускается автоматически)
ollama serve
```

Ollama должен быть доступен на `http://localhost:11434`

## 🚀 Запуск сервера

### Вариант 1: Gradle (рекомендуется)

```bash
cd embedding-server

# Linux/macOS
./gradlew run

# Windows
gradlew.bat run
```

### Вариант 2: Сборка JAR

```bash
./gradlew build
java -jar build/libs/embedding-server-1.0.0.jar
```

Сервер запустится на **http://localhost:8080**

## 📖 API Endpoints

### Эмбеддинги

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/api/embed` | Создать эмбеддинг и сохранить в БД |
| `POST` | `/api/embed/batch` | Создать эмбеддинги для нескольких текстов |
| `POST` | `/api/embed/query` | Создать эмбеддинг без сохранения |
| `POST` | `/api/search` | Семантический поиск |
| `GET` | `/api/embeddings` | Получить все сохранённые эмбеддинги |
| `GET` | `/api/embeddings/{id}` | Получить эмбеддинг по ID |
| `DELETE` | `/api/embeddings/{id}` | Удалить эмбеддинг |

### Документы

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/api/documents/upload` | Загрузить MD-документ |
| `POST` | `/api/documents/ask` | Задать вопрос с RAG |
| `GET` | `/api/documents` | Получить список всех документов |
| `GET` | `/api/documents/{id}` | Получить информацию о документе |
| `GET` | `/api/documents/{id}/chunks` | Получить чанки документа |
| `GET` | `/api/documents/{id}/chunks/{chunkIndex}` | Получить конкретный чанк |
| `DELETE` | `/api/documents/{id}` | Удалить документ |
| `GET` | `/api/documents/stats` | Статистика по документам |

### Система

| Метод | Путь | Описание |
|-------|------|----------|
| `GET` | `/api/health` | Проверка здоровья |
| `GET` | `/api/stats` | Статистика |

## 🎯 Примеры использования

### Создать эмбеддинг

```bash
curl -X POST http://localhost:8080/api/embed \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет, мир!"}'
```

**Ответ:**
```json
{
  "id": 1,
  "text": "Привет, мир!",
  "embedding": [-0.123, 0.456, -0.789, ...],
  "chunks": null
}
```

### Batch обработка

```bash
curl -X POST http://localhost:8080/api/embed/batch \
  -H "Content-Type: application/json" \
  -d '{
    "texts": [
      "Первый текст",
      "Второй текст",
      "Третий текст"
    ]
  }'
```

### Семантический поиск

```bash
curl -X POST http://localhost:8080/api/search \
  -H "Content-Type: application/json" \
  -d '{
    "query": "машинное обучение",
    "topK": 5
  }'
```

### Проверить здоровье

```bash
curl http://localhost:8080/api/health
```

**Ответ:**
```json
{
  "status": "healthy",
  "ollama": true,
  "database": true
}
```

## 📚 Работа с документами

Сервер поддерживает загрузку MD-документов, их автоматическую векторизацию и интеллектуальный поиск с возвратом кликабельных ссылок на источники.

### Основные возможности

1. **Загрузка MD-файлов** - документы автоматически разбиваются на чанки, векторизуются и сохраняются в БД
2. **Семантический поиск** - поиск релевантных фрагментов по векторному сходству
3. **RAG с источниками** - ответы на вопросы с кликабельными ссылками на источники
4. **Управление документами** - просмотр, удаление документов и их чанков

### API Endpoints для документов

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/api/documents/upload` | Загрузить MD-документ |
| `GET` | `/api/documents` | Получить список всех документов |
| `GET` | `/api/documents/{id}` | Получить информацию о документе |
| `GET` | `/api/documents/{id}/chunks` | Получить чанки документа |
| `GET` | `/api/documents/{id}/chunks/{chunkIndex}` | Получить конкретный чанк |
| `POST` | `/api/documents/ask` | Задать вопрос с RAG |
| `DELETE` | `/api/documents/{id}` | Удалить документ |
| `GET` | `/api/documents/stats` | Статистика по документам |

### Примеры работы с документами

#### Загрузка документа

```bash
curl -X POST http://localhost:8080/api/documents/upload \
  -H "Content-Type: application/json" \
  -d '{
    "fileName": "example.md",
    "content": "# Заголовок\n\nТекст документа..."
  }'
```

**Ответ:**
```json
{
  "documentId": 1,
  "fileName": "example.md",
  "fileSize": 1234,
  "chunksCreated": 5,
  "createdAt": "2025-11-28T00:23:57.44617"
}
```

#### Задать вопрос с RAG (главная функция)

```bash
curl -X POST http://localhost:8080/api/documents/ask \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Какие возможности есть у сервера?",
    "topK": 3
  }'
```

**Ответ:**
```json
{
  "question": "Какие возможности есть у сервера?",
  "answer": "Сервер имеет следующие основные возможности...",
  "sources": [
    {
      "documentId": 1,
      "documentName": "example.md",
      "chunkIndex": 0,
      "text": "Фрагмент текста из документа...",
      "similarity": 0.85,
      "similarityPercent": "85.0%",
      "link": "http://localhost:8080/api/documents/1/chunks/0"
    }
  ]
}
```

#### Получить конкретный чанк по ссылке

```bash
# Используйте ссылку из поля "link" в ответе RAG
curl http://localhost:8080/api/documents/1/chunks/0
```

### Как работает RAG с документами

1. **Загрузка документа:**
   - Документ разбивается на чанки размером 100-256 токенов с перекрытием 25 токенов
   - Каждый чанк векторизуется через Ollama (модель nomic-embed-text)
   - Чанки с метаданными сохраняются в базу данных

2. **Поиск с RAG:**
   - Вопрос пользователя векторизуется
   - Выполняется поиск топ-K наиболее похожих чанков по косинусному сходству
   - Найденные чанки используются как контекст для генерации ответа через LLM
   - Возвращается ответ вместе со ссылками на источники

3. **Кликабельные ссылки:**
   - Каждый источник содержит поле `link` с прямой ссылкой на чанк
   - По этой ссылке можно получить полный текст чанка с метаданными

## 📊 Swagger UI

Откройте в браузере: **http://localhost:8080/swagger**

Здесь можно интерактивно тестировать все API методы.

## ⚙️ Настройки

| Параметр | Значение | Описание |
|----------|----------|----------|
| Chunk size | 500-1000 токенов | Размер чанков для длинных текстов |
| Overlap | 50-100 токенов | Перекрытие между чанками |
| Normalization | [-1, 1] | Диапазон нормализации векторов |
| Model | nomic-embed-text | Модель Ollama |
| Dimension | 768 | Размерность эмбеддинга |

## 🏗 Архитектура

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Client    │────▶│   Ktor      │────▶│  Ollama     │
│  (HTTP)     │     │   Server    │     │  (LLM)      │
└─────────────┘     └──────┬──────┘     └─────────────┘
                          │
                          ▼
                   ┌─────────────┐
                   │    H2 DB    │
                   │ (in-memory) │
                   └─────────────┘
```

### Компоненты:

1. **ChunkingService** - разбивает длинные тексты на чанки с перекрытием
2. **OllamaClient** - взаимодействует с Ollama API
3. **NormalizationService** - нормализует вектора к [-1, 1]
4. **EmbeddingRepository** - хранит эмбеддинги в H2 database
5. **EmbeddingService** - объединяет все компоненты

## 📝 Примечания

- **База данных in-memory**: данные не сохраняются при перезапуске сервера
- Для production рекомендуется заменить H2 на PostgreSQL
- Порт по умолчанию: 8080
- Ollama должен быть запущен до старта сервера

## 🔧 Конфигурация Ollama

Если Ollama работает на другом порту или хосте, измените в `OllamaClient.kt`:

```kotlin
class OllamaClient(
    private val baseUrl: String = "http://localhost:11434",  // ← изменить
    private val model: String = "nomic-embed-text"
)
```

## 📄 Лицензия

MIT
